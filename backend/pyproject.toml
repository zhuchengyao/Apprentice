[project]
name = "apprentice-backend"
version = "0.1.0"
description = "Apprentice - AI-powered study companion backend"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
