{
  "id": "layer_norm",
  "category": "neural_networks",
  "difficulty": 2,
  "summary": "SINGLE_FOCUS: 4×8 grid of samples × features colored BLUE_E→RED via tanh(v/2). ValueTracker s_tr morphs from raw (row means differ) to per-sample-normalized (μ_i=0, σ_i=1 per row). Live per-row μ, σ panel + title.",
  "tags": ["layer_norm", "normalization", "value_tracker", "always_redraw"],
  "apis": ["Square", "VGroup", "ValueTracker", "always_redraw", "interpolate_color", "MathTex", "Tex"]
}
